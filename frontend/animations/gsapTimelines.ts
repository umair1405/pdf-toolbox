// GSAP animations disabled - static UI only

export function revealOnScroll(selector: string, opts?: { stagger?: number }) {
  // No-op
}

export function countUp(el: Element, target: number, opts?: { decimals?: number; suffix?: string }) {
  // No-op
}
